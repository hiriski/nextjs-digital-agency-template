'use client'

import Box from '@mui/material/Box'

const PageLoader = () => {
  return (
    <Box
      sx={{
        width: '100%',
        minHeight: 100,
      }}
    ></Box>
  )
}

export default PageLoader
